# Legacy Training Data

This folder preserves small tracked CSV files used by earlier classifier or
sentence-cleaning experiments. The files were moved without modification.

Before using these data for new modeling, review provenance, licensing,
label definitions, and whether the rows still match the current
`litdatamatcher` schemas.
