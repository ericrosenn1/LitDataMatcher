# v0.4 campaign authorization record

The owner authorized a new LitDataMatcher v0.4 compiler campaign on 2026-09-13.
It supersedes the older completion state only for this new feature scope and
does not alter the frozen alpha artifacts, sealed holdout, original checkout,
or the paused supervisor. The requested development setting is GPT-5.6 Terra
with High reasoning. This active session exposes no supported model-selection
or telemetry control, so no model switch is claimed. No paid API, cloud credit,
schedule, self-relaunch loop, force-push, main merge, or public release is
authorized.

The campaign state at `project_state/V0_4_COMPILER_CAMPAIGN_STATE.json` keeps
the prior policy and completion records as history and records the 14-day
review boundary. Its status is intentionally `IMPLEMENTED_NOT_FULLY_VALIDATED`;
the outstanding corpus, benchmark, learning, held-out evaluation, packaging and
independent review gates are not being relabelled as complete.
