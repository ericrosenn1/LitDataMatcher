# LitDataMatcher v2: final Codex build assignment

Specification version: 2.0.0 | Prepared: 2026-09-07

**Outcome:** build, execute, evaluate, improve, and deliver a functional local LitDataMatcher alpha, then continue to a hardened alpha. This package is the build assignment, not the completed application.

## Start

1. Extract this whole folder somewhere Codex can read, preferably Downloads. Keep the companion files together.
2. Open the actual LitDataMatcher local project in Codex. Do not open a disposable auxiliary-agent benchmark snapshot as the project.
3. Select GPT-6 Astra High as the initial substantive-work configuration where available, Standard speed, and use the supported long-running/Goal surface. Available controls must be checked in the installed client. The build may use Extra High or Max selectively when sustainable; Ultra is excluded.
4. Paste `LAUNCH_IN_CODEX.txt` into that session. It instructs Codex to verify and read this package, find and preserve the current project, use actual Codex subagents, and continue through implementation and hardening.
5. The agent should prepare and test the project-local recovery mechanism early. `10_SCHEDULED_SUPERVISOR_PROMPT.md` contains its separately activatable hourly supervisor instruction. That file is not an installed schedule. Only activate a supported local schedule after its access, single-writer behavior, repair ability, and resume path have been demonstrated.

Official product references checked for this package are recorded in `15_SOURCE_NOTES_AND_DECISIONS.md`. `/goal` is documented for supported local clients; scheduled management is a separate surface. A prompt does not extend session limits, grant permissions, or keep a powered-off computer running.

## Final decisions

- Codex is the lead and the only delegated development reasoning/coding system. Do not call OpenCode or Muse or repeat those experiments.
- Reuse existing LitDataMatcher work and qualified external software/data. This is a substantial redesign inside the existing project, not a new empty repository.
- All ten workstreams must be implemented and exercised on real inputs. The first working demonstration is an intermediate milestone.
- Prefer continuous useful progress at the highest sustainable reasoning effort. Parallelize independent work; do not create deliberation or polling just to remain busy.
- Automatic local commits and pushes to a dedicated development branch are authorized. No force pushes, main-branch merges, remote releases, or public dataset uploads.
- Preserve newer local/uncommitted work. Historical paths and GitHub main are clues, not authority over a verified active checkout.
- Maintain compact state, manifests, decisions, and test evidence. Do not create duplicate handoff folders after every operation.
- No additional purchases or paid runtime/API consumption without a separate explicit budget.

## Authority and reading map

`01_MASTER_CODEX_BUILD_PROMPT.md` is the master instruction. Files 02-14 define its operational contracts, 15 records evidence and corrections, and 16 defines the runtime model boundary. `templates/BUILD_SPEC.json` is the machine-readable counterpart for stable requirements and numeric coverage gates. Templates start unconfigured; never overwrite live state with them on resume.

Read the numbered documents once before substantial edits. Delegate focused excerpts rather than repeatedly copying this entire package into every worker. Do not invent requirements from the evidence appendix. Its optional-drafting wording is historical; this finalized assignment excludes both auxiliary agent applications.

Platform/security instructions and later explicit user directions take priority. This package supersedes the older v1 build task and earlier no-commit/no-push and auxiliary-agent advice. Existing unrelated project rules remain in effect. Resolve a genuine conflict once, without repeatedly reopening settled execution choices.

## Package verification

`tools/verify_package.py` checks SHA-256, lengths, safe paths, duplicate archive names, and the full manifest. It uses Python's standard library and neither installs packages nor runs the build. It can verify an extracted folder or this ZIP. It verifies integrity, not scientific validity or application readiness.

The combined downloadable master prompt contains the same instruction as `01_MASTER_CODEX_BUILD_PROMPT.md`. The launch text is a short entry instruction; it is not a substitute for reading the companions.
