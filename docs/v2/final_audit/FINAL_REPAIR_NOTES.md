# Final independent-review repairs

The independent review reproduced six findings, including a high-severity
lifecycle omission. PubMed publication types and direct CommentsCorrections,
OpenAlex `is_retracted`, and Crossref `update-to` now feed explicit source-scoped
lifecycle relations. Ordinary comments remain metadata. An unavailable PubMed
EFetch lookup keeps summary metadata but exposes partial source failure and
unresolved lifecycle review. Provider error objects and invalid envelopes are
never successful empty searches; valid ENA rows survive with visible schema drift.

Python3.10 hashing uses a streaming helper. Biological unit status remains unknown
unless a source-located exact/synonym observation establishes it; ambiguous donor
links override assertions. Known canonical IDs emitted by the local ontology can
be consumed in the same qualified category; unknown IDs remain unresolved.

Mixed recognized/unrecognized assay lists cannot prove a different modality absent.
Explicit pooled CRISPR/RNAi screen metadata supports the perturbational-screen
family. Precise assay names remain distinct; screens do not establish donor counts,
independence, causal answerability, normalization or statistical adequacy. This is
a tested metadata contract, not a claim that a raw screen dataset was acquired.
Genetics/GWAS summary statistics remain unqualified without a source-specific
allele, ancestry, cohort-overlap and endpoint contract; labels are not substituted.

The first six-case new local inference attempt produced zero accepted claims and
zero dossiers. It is retained as a failed development attempt. Source structured
abstracts now retain actual heading/offset information; horizontal Unicode
whitespace normalization is an explicit derivative transformation. The source-span
guard preserves newline boundaries after headings and still rejects omitted
negation/context. A new successful case receipt is required; this document does
not certify the model or final application acceptance.
