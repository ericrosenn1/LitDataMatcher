# Models

Model artifacts in this repository are preserved for provenance unless
otherwise documented. The canonical deterministic package does not require a
tracked model artifact to run.

Legacy binary artifacts live under `models/legacy/`. Future model files should
include training data provenance, evaluation metrics, intended use, and
regeneration instructions before they are used in production analyses.
