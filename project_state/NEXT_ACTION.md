Build active: bootstrap. Canonical lead C:/Codex/LitDataMatcher-v2/lead. Preserve original checkout. Next install isolated environment, run existing regression once, then implement fresh local semantic runtime and live acquisition in isolated writer worktrees. Baseline command proposed: uv sync --extra dev. All product gates NOT_RUN. Supervisor NOT_CONFIGURED. No background schedule exists.
