# Maintenance Scripts

This folder contains small utilities and scratch probes preserved from earlier
development. They are not part of the canonical production package.

Before reusing a script here, check paths, expected inputs, and whether the
logic should instead become a tested package function or a documented command.
