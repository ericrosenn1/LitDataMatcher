# Training

This folder contains preserved training and model-development scripts. These
scripts are not part of the canonical production package and should be treated
as experimental until their inputs, outputs, metrics, and model provenance are
fully documented.

Current production ranking and extraction logic lives in `litdatamatcher/`.
Future training work should write reproducible inputs, labels, metrics, and
model cards before producing new model artifacts.
